node_modules
dist
dist-ssr
*.local
.DS_Store
.vite
*.log
*.tsbuildinfo

# Android / Gradle
android/.gradle/
android/build/
android/*/build/
android/local.properties
android/.idea/
android/captures/
*.apk
*.aab
.kotlin/

# Signing — never commit
android/keystore.properties
*.jks
*.keystore

# Pristine copies of published materials, kept so a restamp never double-marks.
materials-source/

# Local build configuration. Holds the project keys; never committed.
.env
.env.local
.env.*.local
